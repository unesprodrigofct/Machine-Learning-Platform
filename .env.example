# Use a local path for the MVP. Replace with an S3-compatible URI in cloud deployments.
ML_PLATFORM_ARTIFACTS_PATH=artifacts

# Example database connection. Do not commit real credentials.
DATABASE_URL=postgresql+psycopg://username:password@host:5432/database
